#####R libraries#####
#CRAN library
#install.packages(MASS);library(MASS)
#install.packages(plyr);library(plyr)
#install.packages(glmnet);library(glmnet)
#install.packages(pscl);library(pscl)
#install.packages(aster);library(aster)

